#!/usr/bin/env python

import sys

if len(sys.argv) == 1:
    print "Please enter a script to run"
    sys.exit()
elif len(sys.argv) > 2:
    print "Too many arguments"
    sys.exit()
else:
    execfile(sys.argv[1])


